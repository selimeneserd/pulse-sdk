# AI-agent setup recipe / AI-agent kurulum tarifi

Use this alongside the existing Cloud onboarding agent prompt, whose published0.1 path remains supported. This recipe is for the published 0.2.0 SDK; it never requests secrets in chat.

> Inspect this project's instructions, license, package manifests, existing transport and installed MCP version. Preserve dirty work. Do not upgrade MCP to satisfy Pulse. Check the actual compatibility matrix and publication status. Preview a thin integration file with `pulse init --dry-run`; do not rewrite handlers heuristically. Start with an explicitly selected memory/JSONL exporter and a harmless local fixture. Wrap before tool registration, preserve all return/error/cancellation semantics, and integrate bounded flush/shutdown with the existing app lifecycle. Run a real client call and verify original result plus sanitized event. Report static compatibility separately from observed handler/queue/export/collector evidence. If Cloud is selected explicitly, configure the generic HTTP exporter using existing server environment references; never ask for or print a secret, never read customer tools without permission. Do not deploy/publish or send messages. Explain unsupported versions and required manual decisions instead of fabricating success.

TR: Talimatları/lisansı/kurulu MCP sürümünü incele; kullanıcının değişikliklerini koru. MCP'yi zorla yükseltme. Thin entegrasyonu önizle, ham handler kodunu tahminle değiştirme. İlk doğrulamayı anahtarsız yerel örnekle yap. Gerçek çağrı sonucu ve arındırılmış olayı kontrol et; statik bilgiye runtime başarısı deme. Cloud açık seçilirse yalnız mevcut sunucu env referanslarını kullan; chat'te secret isteme/yazma. Publish/deploy/mesaj gönderme.
